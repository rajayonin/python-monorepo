[project]
name = "python-monorepo"
version = "0.1.0"
description = "Simple example of multiple services managed with uv"
requires-python = ">=3.12"
authors = [{ name = "Luis Daniel Casais", email = "lcasais@pa.uc3m.es" }]
dependencies = []

[tool.uv.sources]
common = { workspace = true }

[tool.uv.workspace]
members = ["src/service1", "src/service2", "src/common"]